apiVersion: v1
kind: Service
metadata:
  name: chat-app-frontend-svc
  namespace: chat-app-ns
spec:
  selector:
    app: chat-app-frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer